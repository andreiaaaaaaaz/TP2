#include <stdio.h>

int main()
{
    int i = 1;

    // Loop while i is less than or equal to 100
    while (i <= 100)
    {
        // Print the current value of i
        printf("%d\n", i);

        // Increment i by 1
        i++;
    }

    return 0;
}
